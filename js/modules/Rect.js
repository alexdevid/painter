Game.prototype.Rect = function(w, h, pos, style) {

    var private = {
        w: w,
        h: h,
        pos: pos,
        style: style,
        
        object: function() {
            
        }
    };

    return private;
};